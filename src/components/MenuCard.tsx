export interface MenuItem {
    name: string,
    imageSrc: string,
    description: string,
    price: number
}

export const MenuCard = ({ menuItem }: { menuItem: MenuItem }) => {
    return <></>
}